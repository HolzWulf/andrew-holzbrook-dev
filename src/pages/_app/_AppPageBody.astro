---
import Box from "../../lib/ui/Box.astro";

interface Props {
    class?: string;
    backgroundColor?: string;
}

const props: Props = Astro.props;
const className = props.class ?? "";
const backgroundColor = props.backgroundColor ?? "bg-gray-100";
---

<Box class={`${backgroundColor} ${className}`}>
    <slot />
</Box>
