---
import Stack from "../../lib/ui/Stack.astro";

interface Props {
    class?: string;
}

const props: Props = Astro.props;
const className = props.class ?? "";
---

<Stack class={`h-screen bg-gray-100 py-8 px-4 ${className}`}>
    <slot />
</Stack>
