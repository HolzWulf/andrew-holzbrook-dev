---
import FamilyMemberListItem from "./FamilyMemberListItem.astro";
import { familyMembers } from "../_familyMembers";
---

<ul>
  {
    familyMembers.map((familyMember) => (
      <FamilyMemberListItem familyMember={familyMember} />
    ))
  }
</ul>
